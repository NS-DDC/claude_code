# VisionAce

**VisionAce** - Deep Learning Labeling & Training Tool

딥러닝을 위한 강력한 이미지 라벨링 및 학습 도구입니다.

## 🌟 주요 기능

### 라벨링 도구
- **Detection (BBox)**: 객체 탐지를 위한 바운딩 박스 라벨링
  - Rectangle 모드: 드래그로 박스 그리기
  - Polygon 모드: 다각형 바운딩 박스
- **Segmentation (Mask)**: 인스턴스 세그멘테이션을 위한 마스크 라벨링
  - 브러시 도구 (원형/사각형)
  - 폴리곤 도구
  - 브러시 크기 조절 가능

### 편의 기능
- **자동 라벨링**: 학습된 모델로 전체 이미지 자동 라벨링
- **모델 학습**: YOLO 기반 모델 학습 기능 내장
- **다국어 지원**: 한국어 / English
- **최근 항목**: 최근 작업 폴더 및 모델 빠른 접근
- **실행 취소/다시 실행**: 작업 히스토리 관리
- **클래스별 색상**: 클래스마다 고유한 색상 설정

## ⌨️ 키보드 단축키

### 도구 선택
- **Q**: 선택/편집 모드
- **W**: Detection (BBox) 모드
- **E**: Segmentation (Mask) 모드

### 이미지 탐색
- **A**: 이전 이미지
- **S**: 저장 후 다음 이미지
- **D**: 저장 안하고 다음 이미지
- **X**: 다음 이미지로 건너뛰기 (자동저장 무시)

### 편집
- **Ctrl+Z**: 실행 취소
- **Ctrl+Y**: 다시 실행
- **Delete**: 선택 라벨 삭제
- **Enter**: 폴리곤/마스크 완료
- **Esc**: 현재 작업 취소

### 기타
- **Ctrl+O**: 이미지 폴더 열기
- **Ctrl+S**: 라벨 저장 (툴바 확장자로)
- **F**: 학습에서 제외 (이미지/라벨/GT 삭제)
- **F1**: 도움말 패널 토글
- **Ctrl+Q**: 종료

### 마우스
- **휠**: 확대/축소
- **Ctrl+휠**: 브러시 크기 조절
- **중간 버튼**: 화면 이동
- **좌클릭**: 선택/그리기
- **우클릭**: 지우기 / 폴리곤 완료

## 📁 출력 폴더 구조

```
project_folder/
├── images/         # 라벨링 완료된 원본 이미지
├── labels/         # YOLO 포맷 라벨 파일 (.txt)
└── gt_image/       # 세그멘테이션 마스크 (클래스별 폴더)
    ├── class1/
    ├── class2/
    └── ...
```

## 🚀 사용 방법

### 1. 이미지 폴더 열기
- `Ctrl+O` 또는 `파일 > 이미지 폴더 열기`

### 2. 클래스 추가
- 라벨 패널에서 "클래스 추가" 버튼 클릭
- 클래스 이름 입력
- 더블클릭으로 색상 변경 가능

### 3. 라벨링 수행
- 도구 선택: Q(선택), W(BBox), E(Segmentation)
- BBox: 드래그로 박스 그리기
- Segmentation: 브러시로 마스크 그리기

### 4. 저장
- `S` 키로 저장 후 다음 이미지
- `Ctrl+S`로 수동 저장
- 자동 저장 활성화 가능

### 5. 모델 학습 (선택)
- `도구 > 학습...`
- 데이터셋 경로 및 파라미터 설정
- 학습 시작

## 🔧 설치 및 실행

### 필요 패키지
```bash
pip install PySide6 opencv-python numpy torch torchvision ultralytics
```

### 실행
```bash
python main.py
```

### 빌드 (PyInstaller)
```bash
pyinstaller visionace.spec
```

## 📋 지원 포맷

### 이미지
- 입력: `.jpg`, `.jpeg`, `.png`, `.bmp`, `.tif`, `.tiff`
- 출력: 원본 확장자 / PNG / JPG (선택 가능)

### 모델
- PyTorch: `.pt`
- Keras: `.h5`

### 라벨
- YOLO 포맷 `.txt` (normalized coordinates)

## 🛠️ 기술 스택

- **GUI**: PySide6 (Qt)
- **Deep Learning**: PyTorch, Ultralytics YOLO
- **이미지 처리**: OpenCV, NumPy
- **언어**: Python 3.8+

## 📝 라이선스

MIT License

## 🤝 기여

이슈 제보 및 풀 리퀘스트는 언제나 환영합니다!

## 📧 연락처

문의사항이 있으시면 이슈를 등록해주세요.

---

**VisionAce** - Make your labeling work easier and faster! 🚀
